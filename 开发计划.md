# 弹砖块（Breakout）MVP 开发计划

> 本文件是项目唯一的开发依据。后续每一步开发都严格按此执行，每完成一步就停下来 review / commit。
> 文档中的 API 均已对照本地 `node_modules/phaser`（**4.2.1**）类型定义核实过，优先以本文件为准，不要照搬 Phaser 3 的网上教程。

## 1. 项目概述

- **目标**：做一个能玩的经典打砖块 MVP。
  - 一个关卡：底部挡板左右移动接球，球撞碎顶部砖块，球落底扣一条命；清空全部砖块过关，命用完游戏结束。
  - 美术用纯色块（Phaser Shape 图形对象），不加载图片，先把逻辑跑通。
- **技术栈**：Phaser 4.2.1 + Vite + TypeScript（**不引入 React**）。
- **分工原则**：所有逻辑、UI（分数/生命文字）都在 Phaser 内完成，用 `this.add.text` / Shape 直接画在画布上。

## 2. 目录结构（目标形态）

```
src/
  game/
    config.ts          // Phaser.Game 配置 + 场景注册（含 arcade 物理，重力为 0）
    constants.ts       // 画布尺寸、挡板/球/砖块尺寸、速度、颜色、行列数、生命数
    data/
      levels.ts        // 砖块布局数据（纯数据：每行颜色等）
    scenes/
      GameScene.ts     // 主场景：渲染挡板/球/砖块、物理、输入、记分、生命、胜负
    utils/
      brick.ts         // 布局数据 → 砖块坐标/颜色的解析函数
  main.ts              // Vite 入口：new Phaser.Game(config)
  index.html           // 挂载容器（<div id="app">）+ 引入 main.ts
```

## 3. 核心约定（贯穿所有步骤，务必遵守）

### 3.1 数据驱动：布局数据与运行时分离

- `levels.ts` 只存纯数据（行数、每行颜色、列数等）；运行时由 `utils/brick.ts` + `constants.ts` 的尺寸算出每个砖块的坐标、尺寸、颜色。
- 记分 `score`、生命 `lives`、是否已发射 `launched`、剩余砖块数 `remaining` 等**可变状态不写进布局数据**，只在场景里维护。

### 3.2 坐标与魔法数字

- 画布固定 `800 × 600`，左上角原点，全部用像素坐标。
- 所有尺寸/间距/速度/颜色定义为 `constants.ts` 里的常量，**禁止**散落魔法数字。

### 3.3 物理约定（重要）

- **球**：动态 body，`velocity` 驱动。
- **挡板、砖块**：静态 body（`existing(obj, true)`），不被球推动，只做反弹。
- 反弹能量不衰减：球 `setBounce(1, 1)`；墙壁反弹手动反转速度分量（见 4.5），保证速度绝对值不变。

### 3.4 restart 状态重置

- `scene.restart()` 复用同一场景实例，类字段初始化器（`score = 0`、`lives = 3`）只在构造时跑一次、重开不会重跑。**所有可变状态必须在 `create()` 开头显式重置**。
- 事件驱动优先：球未发射时 `update()` 只做落底检测；挡板移动由 `pointermove` 事件触发，不逐帧轮询。

### 3.5 命名与注释

- 场景类名 `GameScene`，key 用同名 `'GameScene'`。
- 坐标换算、碰撞回调、落底判定处写简短中文注释，方便 review。

## 4. Phaser 4 关键 API 速查（已核实）

### 4.1 创建游戏（含 arcade 物理，无重力）

```ts
import Phaser from 'phaser';

new Phaser.Game({
  type: Phaser.AUTO,
  width: 800,
  height: 600,
  parent: 'app',
  backgroundColor: '#101014',
  physics: { default: 'arcade', arcade: { gravity: { y: 0 }, debug: false } },
  scene: [GameScene],
});
```

> Phaser 4 主包默认同时包含 Arcade 与 Matter 物理，`import Phaser from 'phaser'` 后 `this.physics` 直接可用，无需额外 import。

### 4.2 定义场景

```ts
export class GameScene extends Phaser.Scene {
  constructor() { super({ key: 'GameScene' }); }
  create(): void { /* 初始化、渲染、挂物理 */ }
  update(time: number, delta: number): void { /* 落底检测等 */ }
}
```

### 4.3 图形（Shape，MVP 不加载图片）

```ts
const rect = this.add.rectangle(x, y, w, h, 0xff4d4d, 1).setOrigin(0);
const circle = this.add.circle(x, y, radius, 0xffffff); // 默认中心原点
```

### 4.4 给 Shape 挂物理 body

```ts
// 球：动态 body
this.ball = this.add.circle(x, y, BALL_R, 0xffffff);
this.physics.add.existing(this.ball);
const body = this.ball.body as Phaser.Physics.Arcade.Body;
body.setCircle(BALL_R);           // 圆形 body 必须标成圆
body.setBounce(1, 1);

// 挡板 / 砖块：静态 body（第二个参数 true）
this.paddle = this.add.rectangle(x, y, PADDLE_W, PADDLE_H, 0xffffff);
this.physics.add.existing(this.paddle, true);
```

### 4.5 速度与墙壁反弹

```ts
body.setVelocity(250, -320);                      // 发射
body.velocity.x = -Math.abs(body.velocity.x);     // 向右弹 → 反向
body.velocity.y = -Math.abs(body.velocity.y);     // 向上弹
```

### 4.6 碰撞

```ts
this.physics.add.collider(ball, paddle, this.hitPaddle, undefined, this);
this.physics.add.collider(ball, bricksGroup, this.hitBrick, undefined, this);
// 砖块可用 this.physics.add.staticGroup()，或普通 Group + 每个成员挂静态 body
```

### 4.7 指针输入（挡板跟随 + 点击发射）

```ts
this.input.on('pointermove', (p: Phaser.Input.Pointer) => {
  this.paddle.x = Phaser.Math.Clamp(p.x, minX, maxX);
});
this.input.on('pointerdown', () => { if (!this.launched) this.launch(); });
```

### 4.8 文本与场景切换

```ts
this.add.text(x, y, '分数: 0', { fontSize: '24px', color: '#ffffff' });
this.scene.restart();
```

> 本次 MVP 不涉及 Phaser 4 相对 v3 的常见变化（Filters、setTintMode、RenderNode、Light2D 等），后续加特效时再查迁移指南。

## 5. 开发步骤

> 每步完成后：`npm run dev` 手动验证「验证点」，再 review 代码，最后 commit。

### 步骤 1：项目骨架 + 画布

**目标**：页面出现一块 800×600 的游戏画布，跑通 Vite + Phaser 4 + arcade 物理。

**要做的事**：

1. 初始化 Vite + TS 项目（`npm create vite@latest . -- --template vanilla-ts`），装 `phaser@^4.2.1`。
2. 清掉脚手架默认 `counter` 演示代码，`index.html` 里放 `<div id="app">`，`main.ts` 里 `new Phaser.Game({...})`。
3. 建 `src/game/config.ts`（4.1 的配置，先只注册空 `GameScene`）和 `src/game/constants.ts`（先放画布尺寸、颜色）。
4. 建 `src/game/scenes/GameScene.ts`，`create()` 里 `this.add.rectangle(...)` 画一个色块 + `this.add.text(...)` 画一行提示。

**关键 API**：`new Phaser.Game`、`this.add.rectangle`、`this.add.text`、`physics.default: 'arcade'`。

**验证点**：页面显示 800×600 画布、色块和文字，控制台无报错，`this.physics` 可用（打印 `this.physics.world` 不为 undefined）。

---

### 步骤 2：挡板跟随指针

**目标**：底部出现一块白色挡板，随鼠标/手指左右移动，不超出左右边界。

**要做的事**：

1. `constants.ts` 加 `PADDLE_W = 120`、`PADDLE_H = 16`、`PADDLE_Y = 560`。
2. `GameScene.create()` 里画挡板矩形，挂静态 body（`existing(paddle, true)`）。
3. 监听 `pointermove`，`paddle.x = Phaser.Math.Clamp(p.x, PADDLE_W/2, WIDTH - PADDLE_W/2)`。

**关键 API**：`this.physics.add.existing(obj, true)`、`this.input.on('pointermove')`、`Phaser.Math.Clamp`。

**验证点**：挡板跟随指针左右滑，滑到最左/最右时被 `Clamp` 挡住不越界。

---

### 步骤 3：球 + 点击发射

**目标**：球初始贴在挡板上方，点击后向上发射。

**要做的事**：

1. `constants.ts` 加 `BALL_R = 8`、`BALL_SPEED_X = 250`、`BALL_SPEED_Y = -320`。
2. `create()` 画圆形球、挂动态 body、`setCircle(BALL_R)`、`setBounce(1, 1)`，先 `body.enable = false`（暂不参与物理）并让球位置跟随挡板。
3. 维护 `launched = false`；`pointerdown` 时若未发射，把球放到挡板上方、`body.enable = true`、`setVelocity(BALL_SPEED_X, BALL_SPEED_Y)`、置 `launched = true`。

**关键 API**：`this.add.circle`、`this.physics.add.existing`、`body.setCircle`、`body.enable`、`body.setVelocity`。

**验证点**：球贴在挡板上随挡板移动；点击后球以斜向上速度飞出。

---

### 步骤 4：墙壁/挡板反弹 + 落底扣命

**目标**：球撞左/右/上墙反弹、撞挡板反弹朝上，落底扣命并重新发球。

**要做的事**：

1. 在 `update()` 里手动做墙反弹（不用 `setCollideWorldBounds`，因为底边要“掉下去”而不是弹回）：
   - `ball.x <= BALL_R && body.velocity.x < 0` → `velocity.x = Math.abs(...)`
   - `ball.x >= WIDTH - BALL_R && body.velocity.x > 0` → `velocity.x = -Math.abs(...)`
   - `ball.y <= BALL_R && body.velocity.y < 0` → `velocity.y = Math.abs(...)`
2. 加 `this.physics.add.collider(ball, paddle, hitPaddle)`；`hitPaddle` 里 `body.velocity.y = -Math.abs(body.velocity.y)`（保证朝上），可加 2 行按击中位置调 `velocity.x` 的“手感”逻辑。
3. 落底检测：`ball.y > HEIGHT + BALL_R` → `lives--`、`launched = false`、球回到挡板上方；`lives <= 0` 显示“游戏结束”。

**关键 API**：`update()`、`body.velocity`、`this.physics.add.collider`、`Phaser.Math.Clamp`。

**验证点**：球左右上墙与挡板反弹方向正确、速度不衰减；球落底后扣命并重新贴在挡板上等待发射；命用完显示结束。

---

### 步骤 5：砖块布局 + 渲染

**目标**：顶部铺出一片彩色砖块墙，每块挂静态 body。

**要做的事**：

1. `constants.ts` 加 `COLS = 8`、`ROWS = 5`、`BRICK_W = 84`、`BRICK_H = 24`、`BRICK_GAP = 4`、`BRICKS_TOP = 60`、`BRICK_COLORS = [...]`（每行一个色）。
2. `data/levels.ts` 定义关卡数据（行数 + 每行颜色索引）。
3. `utils/brick.ts` 写 `buildBricks(scene, level)`：根据数据算出每个砖块坐标/颜色，逐个 `this.add.rectangle(...)` + `existing(b, true)`，放进一个 `Phaser.GameObjects.Group`，返回该 Group 和砖块总数。
4. 记录 `remaining = 砖块总数`，供步骤 6 判胜。

**关键 API**：`this.add.rectangle`、`this.physics.add.existing(b, true)`、`this.add.group()`、`Phaser.Math`。

**验证点**：画面铺出 8×5 彩色砖块墙，居中对齐、间距均匀；每块都有静态 body（可临时 `debug: true` 看 body 轮廓）。

---

### 步骤 6：球撞砖块（销毁 + 记分）

**目标**：球碰到砖块，砖块消失、加分、剩余数减一。

**要做的事**：

1. 加 `this.physics.add.collider(ball, bricksGroup, hitBrick, undefined, this)`。
2. `hitBrick(ball, brick)`：`brick.destroy()`、`remaining--`、`score += 10`、更新分数文本。
3. 顶部放 `scoreText`、`livesText`，每次变化刷新文本。

**关键 API**：`this.physics.add.collider`、`brick.destroy()`、`this.add.text` / `text.setText()`。

**验证点**：球撞到的砖块立即消失并 +10 分；分数文本实时更新；球继续正常反弹。

---

### 步骤 7：胜负判定 + 重开

**目标**：清空砖块显示过关；命尽显示结束；可重开。

**要做的事**：

1. `hitBrick` 里当 `remaining <= 0` → 显示“过关！”并停球（`launched = false`、`body.enable = false`）。
2. 命尽（步骤 4）→ 显示“游戏结束”并停球。
3. 加一个常驻「重开」按钮（矩形底 + 文字 + `setInteractive`），点击 `this.scene.restart()`；`create()` 开头显式重置 `score / lives / launched / remaining`（见 3.4）。

**关键 API**：`this.scene.restart()`、`setInteractive().on('pointerdown')`。

**验证点**：清空所有砖块立即显示过关；命尽显示结束；结束后/中途点「重开」回到初始状态，分数归零、砖块恢复、球回到挡板。

## 6. MVP 完成定义（验收标准）

- [ ] 打开页面即可玩，无需额外操作。
- [ ] 挡板随指针左右移动，不越界。
- [ ] 点击发射球，球撞墙/挡板反弹不衰减、方向正确。
- [ ] 球撞砖块：砖块消失、+10 分、分数实时更新。
- [ ] 球落底扣一条命并重新发球；命尽显示游戏结束。
- [ ] 清空全部砖块显示过关。
- [ ] 「重开」按钮随时可重开，状态完整重置。
- [ ] 代码结构符合第 2 节目录，逻辑清晰、有注释，可 review。

## 7. 非 MVP（后续可扩展，本次不做）

- 多关卡 / 选关界面。
- 道具（加宽挡板、多球、穿透球等）。
- 贴图、粒子特效、音效。
- 难度递增（球速随击碎砖块数加快）。
- 砖块血量（打两下才碎）、不同颜色不同分值。
- 高分榜 / 本地存档。

## 8. 踩坑记录（后续每踩一个补一条）

1. **restart 状态残留**：`scene.restart()` 复用场景实例，`score/lives/launched/remaining` 不会自动归零 → 必须在 `create()` 开头重置（见 3.4）。
2. **圆形 body**：`this.add.circle` 挂物理后 body 默认是矩形，必须 `body.setCircle(BALL_R)`，否则墙角处反弹会“卡角”。
3. **底边不能反弹**：打砖块的底边是“掉下去”，不能用 `setCollideWorldBounds(true)`，否则球会在底边弹回、永不掉命——墙壁反弹要在 `update()` 里手动做（见步骤 4）。
